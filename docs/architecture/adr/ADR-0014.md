## ADR-0014 — Dependency Flow and Layer Import Rules

**Status:** Accepted

### Context & Rationale

This project follows Clean Architecture (ADR-0002) and is organized into **Core**, **features**, and **Infra** (ADR-0003).

> **Features is not an architectural layer**. A feature is a *vertical slice* that may contain its own folders for the architectural layers (Application/Domain/Data/Presentation) for specific functionality.

> **Core** encapsulates cross-cutting capabilities as a *vertical slice* structured by architectural layers (Application, Domain, Data, Presentation), enabling reuse across features while preserving feature-level independence.

> **Infra is** an Architectural layer removed from **Core** and **features**, that centralizes all external integrations and platform-specific implementations.

> Even though the module follows Clean Architecture, **Hexagonal Architecture** concepts are also applied to the project, such as the use of **Ports** and **Adapters** as tools to enforce architectural boundaries and dependency rules.

Within `lib/src`, the codebase is structured into `five architectural layers`, each with a clear responsibility and dependency direction:

- `Domain` — business language and rules
- `Application` — orchestration and system intent
- `Data` — persistence-facing implementations and IO translation
- `Infra` — external integrations, SDKs, and platform details
- `Presentation` — UI and stores/state management

This ADR formalizes **dependency flow and import rules** to prevent architectural drift.

---

### Decision

The module follows a **strict inward dependency rule**:

```text
Presentation → Application → Domain
Infra → Data → Application → Domain
```

Notes: 

> The Data layer depends on both Application (contracts only) and Domain.

> Although depicted as 2 flows, this does not represent two pipelines.

> All dependencies still point inward. Data and Infra are both outer layers from the perspective of Application and Domain.

Which means:

- Presentation depends only on Application (and Domain types as read-only data)
- Application depends only on Domain
- Data depends on Application contracts
- Data depends on Domain (entities, failures, repository contracts, etc.)
- Infra depends on Application and Data contracts
- There is no direct dependency from Presentation to Infra or Data.

---

## 1. Domain Layer

The Domain layer:

- MUST NOT depend on any other layer
- MUST NOT import anything from:
  - Application
  - Data
  - Infra
  - Presentation

The Domain contains only **business concepts and rules**, expressed in ubiquitous language:

- entities
- value objects
- domain services
- policies
- failures
- repository contracts

The Domain never references UI, frameworks, SDKs, persistence, or application flow.

Canonical locations:

- `lib/src/core/domain/entities/...`
- `lib/src/core/domain/value_objects/...`
- `lib/src/core/domain/services/...`
- `lib/src/core/domain/policies/...`
- `lib/src/core/domain/failures/...`
- `lib/src/core/domain/repositories/...`
- `lib/src/core/domain/utils/...`

---

## 2. Application Layer

The Application layer:

- MAY depend on Domain
- MUST NOT depend on Data, Infra, or Presentation

The Application layer OWNS:

- use cases (application orchestration)
- ports (interfaces for technical/environment services)
- DTOs required by use cases or ports

Notes:

> Application DTOs are objects only consumed by use cases, not API, database, or UI models.

> Repository contracts belong to the Domain because persistence is a business concern, while technical services (network, device, rate limiting) are application concerns, that's why ports live in Application.

The Application defines **what the system does**, not **how it is delivered or implemented**.

Clarifications:

- Repository contracts live in **Domain**.
- Technical services (network, device, metadata, rate limiting, etc.) are modeled as **Application Ports** (ADR-0011).

Port implementation note:

- **Application Ports may be implemented in Infra or in Data**.
  - Ports that represent **technical/environment integration** (SDK, device, network) are typically implemented in **Infra** as adapters (ADR-0011).
  - Ports that are closely related to data access or persistence concerns that do not depend on specific infrastructure details may be implemented in **Data**.

Canonical locations:

- Use cases (shared): `lib/src/core/application/use_cases/...`
- Use cases (feature): `lib/src/features/<feature>/application/use_cases/...`
- Ports: `lib/src/core/application/ports/...`
- Port DTOs: `lib/src/core/application/dtos/...`

---

## 3. Data Layer

The Data layer acts as a boundary adapter layer, isolating persistence concerns and translating them into domain concepts.

The Data layer:

- MAY depend on:
  - Domain (entities, value objects, failures, repository contracts)
  - Application contracts ONLY
- MUST NOT depend on:
  - Application use cases
  - Presentation
  - Infra implementations

Responsibilities:

- implement Domain repository contracts
- implement Application Ports
- translate technical/IO exceptions into Domain failures
- map data models ↔ domain entities
- isolate persistence concerns from application flow

Canonical locations:

- Data source contracts: `lib/src/core/data/data_sources/...` or `lib/src/features/<feature>/data/data_sources/...`
- Repository implementations: `lib/src/core/data/repositories/...` or `lib/src/features/<feature>/data/repositories/...`
- Data exceptions: `lib/src/core/data/exceptions/...` or `lib/src/features/<feature>/data/exceptions/...`
- Models: `lib/src/core/data/models/...` or `lib/src/features/<feature>/data/models/...`
- Mappers: `lib/src/core/data/mappers/...` or `lib/src/features/<feature>/data/mappers/...`
- Extensions: `lib/src/core/data/extensions/...` or `lib/src/features/<feature>/data/extensions/...`

---

## 4. Infra Layer

The Infra layer contains **technology- and platform-dependent code**.

The Infra layer:

- MAY depend on:
  - Application contracts (ports, DTOs)
  - Data contracts and Data exceptions
  - Domain types only when required by an interface
    > (e.g. `AccessControlService` that requires `GoalsModulePermission` enum/type from Domain policy to integrate the module with the host app - creating a DTO for this would be unnecessary. Also, Infra depending on Domain types does not violate the dependency direction rule defined in this ADR, since Domain has no dependencies and does not import anything from any other layer).
- MUST NOT:
  - Depend on Application use cases
  - Depend on Presentation
  - Orchestrate or control application flow
  - Implement business rules
  - Create, translate or decide Domain failures

Responsibilities:

- implement Application Ports (adapters) (ADR-0011) 
- implement Data Source contracts using SDKs, APIs, OS features
- deal with IO, platform, and third-party integrations

Canonical locations:

- Port adapters: `lib/src/infra/adapters/...`
- SDK-backed data sources: `lib/src/infra/data_sources/...`

---

## 5. Presentation Layer

The Presentation layer is the **outermost layer**.

The Presentation layer:

- MAY depend on:
  - Application (use cases only)
  - Domain (as read-only data types only)
  >  (e.g. The `GoalsModulePermission` enum (from Domain Policies) flag used by the UI to render different widgets based on the user's permissions, which are verified by the `AccessControlService` Application Port implemented in Infra).
- MUST NOT depend on:
  - Data
  - Infra implementations
  - Technical exceptions

Responsibilities:

- UI rendering (widgets, views, screens)
- user input handling
- state management
- mapping user actions → use case execution
- mapping use case results → UI state

Rules:

- Presentation MUST call **Application use cases only**
- Presentation MUST NOT be the source of the truth for business rules
- Presentation MUST NOT access repositories, data sources, or infra services directly

Canonical locations:

- `lib/src/features/<feature>/presentation/...`
- `lib/src/core/presentation/...`

---

## Consequences

- Dependency flow is explicit and enforceable.
- Application controls orchestration and system behavior.
- Domain remains pure and stable.
- Data isolates persistence and technical details.
- Infra contains technology- and platform-dependent details.
- UI changes never force changes inward and it can be easily replaced or refactored without affecting core business logic.
