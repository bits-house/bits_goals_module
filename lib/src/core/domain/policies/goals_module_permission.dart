/// Defines the set of permissions available within the Goals Module.
///
/// This is a domain concept (policy), not an infrastructure concern.
enum GoalsModulePermission {
  /// No special permission required.
  none,

  manageGlobalGoals,
}
